import { useState } from 'react'

function App() {
  return (
    <ul>
      <li>
        <span>(進捗状態) タイトル</span>
        <button>編集</button>
        <button>削除</button>
      </li>
    </ul>
  )
}

export default App
